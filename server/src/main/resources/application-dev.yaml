server:
    port: 8017
    
logging:
    level:
        root: debug

spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: 123456
        username: postgres
        url: jdbc:postgresql://localhost:5432/mytest
